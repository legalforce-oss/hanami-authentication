require "spec_helper"

RSpec.describe Hanami::Auth do
  it "has a version number" do
    expect(Hanami::Auth::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
